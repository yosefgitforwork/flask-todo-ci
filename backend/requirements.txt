# Web framework + extensions (all pinned for reproducible builds)
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Cors==4.0.1
SQLAlchemy==2.0.32

# MySQL driver. PyMySQL is pure-Python; cryptography is required for MySQL 8's
# default caching_sha2_password authentication.
PyMySQL==1.1.1
cryptography==43.0.1

# Production WSGI server
gunicorn==22.0.0
